# 前言

**缺陷**:
1. 可能由于我对Harness的运用还不大熟练，也可能由于AI个别能力不足，AI对using-git-worktrees要求理解有误，未按模块分类worktrees文件夹，创建git feature分支。后经人工干预提醒修正
2. 由于是在window上进行开发，导致linux兼容略有问题，所以网页端linux服务器部署的harness可能略有问题
3. 由于没有使用过网页端的Harness，不知道其具体作用（毕竟网页端无法读与写本地内容），所以只好做成聊天窗口形式，勉强支持git clone获取项目仓库。

# 作用

**评判标准**:由于课程的经费有限，仅支持进行一次完整Harness构建与完善，所以未进行消融实验，各技能的作用大小均为在本次Harness运行的主观感受

**brainstorming**:作用最大的一部分。不仅有助于让AI更好地理解我的需求，也为后续所有项目开发奠定了基础。

**writing-plans**:有较大作用。让AI一口气写完一个Harness，无论是AI能力还是输出上下文都是不可能的。但通过**writing-plans**实现分步依次进行，使得构建较大型项目成为可能。输出的plan也使得subagent-driven-development成为可能。

**using-git-worktrees**:认为“形式大于实质”。正如[前言](#前言)的缺陷所言，该阶段的执行不尽如人意，但最终代码仍能正常运行，构建过程也十分顺利。不过，该阶段的主要目的为使修改可控，构建清晰的工作树，但该项目构建十分顺利，削弱了其作用，

**subagent-driven-development**:有较大作用。子智能体可以分担主智能体的工作，一方面可以使主智能体的上下文更干净一些，只有子智能体的完成报告，另一方面使并行化成为可能

**test-driven-development**:认为“形式大于实质”。
原因:
1. 哪怕应用了TTD，AI写出来的代码仍有不少bug
2. AI写出来的测试用例往往较为浅层，简单
3. 我未提供测试APIkey，AI也未索要APIkey而是用mock LLM，从根本上导致测试用例与真实场景存在差距。
4. 可能在subagent-driven-development阶段有帮助AI修正代码错误，但由于较透明的执行过程导致不太明显，未显示出其重要性。
*本次项目的test为AI设计，一方面是因为本次项目核心是AIForSE，另一方面我认为强制让AI去满足人类写出的复杂test会很困难。*

**requesting-code-review**:有一定作用。能发现subagent-driven-development阶段出现的一些bug

**finishing-a-development-branch**:有一定作用。删除分支整理工作区让工作区更整洁，方便后续开发。（虽然正如[前言](#前言)中所说，AI在执行阶段并没有创建分支，之后人工干预提醒修正，但其意义仍然存在）

# TDD强制定位

更偏向于阻碍一点，首先正如[作用](#作用)中所言，TTD本身“形式大于实质”。此外，让AI编写测试用例本身需要花费token，个人认为性价比低于其他superpower要求，由此，略有阻碍作用。

# subagent-driven 工作流自主运行时间

最长完整运行时间145分16秒，且停止原因为人为停止（即[前言](#前言)中所提到的人工干预）。重新启动后花费5分钟左右完成剩余任务，在完成任务期间均没有偏离主题，稳定性较好。

# task 颗粒度

按模块划分，一般是一个模块一个task，大模块可以分为多个task。一般而言，一个task是完成一个较复杂的函数这样的颗粒度最佳，能力较低的sub-agent可以胜任，又不会过细导致过多调用LLM，增加LLM调用延迟。

不过，说到底PLAN.md是AI输出的，task 颗粒度的最终决定权在AI手上。我也没有要求AI输出不同颗粒度的plan进行对比实验，所以以上仅仅是经验之谈。

# SPEC / PLAN 质量影响实现质量案例

## 起因

在writing-plans阶段之后进行冷启动验证，发现task1的tsconfig.json内容出现缺漏，会导致类型检查不覆盖部分文件。向主Agent反映后进行了修复，将"rootDir"改为根目录而非src目录

## 经过

执行到task23时，原要求为Update package.json bin — "ccg": "dist/cli/index.js"，但把"rootDir"改为根目录之后，应该是Update package.json bin — "ccg": "dist/src/cli/index.js"。但缺乏task1相关信息，子智能体仍然按原要求执行，主智能体也未能在开发阶段发现该错误

## 结果

整个项目编译失败，后经主智能体核查修复

## 总结

由于项目耦合度较高，智能体仅修改一处问题，导致plan上下文不一致，最终导致subagent执行了错误的指令

# 最有效的prompt策略

最有效的prompt策略就是直接使用现成命令，具体而言：
一开始，我使用自然语言命令AI“使用brainstorm技能帮助我进行Harness开发”，收效甚微，未严格按照brainstorm要求进行
随着我对我使用的Harness了解逐渐深入，我发现可以直接使用命令“/brainstorm 帮助我进行Harness开发”使AI进入brainstorm模式，收效显著
所以优先使用Harness已提前为你封装好的prompt是最有效的。

# 凭据与分发

首先第一个需要想清楚的是**凭据 / API Key 的安全存储**要求到底是针对我使用Harness的要求还是用户使用我构建的Harness的要求。
“首次运行应能引导用户安全录入 key”好像是对我的Harness的要求，“key 绝不硬编码进源码、绝不提交进 Git（含历史）”又好像是对我使用Harness的要求。要安全存储的是我的key还是用户的key？
第二个问题是回显问题，**凭据 / API Key 的安全存储**要求回显不显示明文，但这样回显的意义又何在？显示一堆“*”？所以我直接放弃了回显操作，而是直接告知是否已配置，简单直接。
第三个问题是分发问题，我选的是二进制打包文件，这样用户简单直接，但也可能导致配置修改困难。要怎么让二进制文件能灵活地修改配置呢？

# 重做修正

1. 添加智能体崩溃定时自动重启功能
2. CLI形式对初级用户不大友好，重做希望尽量出GUI形式
3. 忽略[作用](#作用)中“形式大于实质”的步骤，进行消融实验
4. 尝试使用目前最强的智能体之一————GPT作为开发智能体，看看其与Deepseek的差距

# Superpowers批判

1. 假设了项目较为复杂，又或者说Superpowers本身就是为了大型项目准备的。但Harness项目大多为最简实现，复杂度大致是中型项目，使得Superpowers的一些功能没有发光发亮
2. 假设了TDD有助于AI开发。虽然没有进行消融没法说什么，但个人认为自己给自己写测试用例会“当局者迷旁观者清”，所以至少建议test由另一个agent设计效果更佳。
3. 假设了AI有能力胜任Superpowers框架。我所使用的AI不能完全胜任Superpowers，导致一些环节出现了疏漏。
